import React from "react";
import type * as svgIcons from "../components/IconComponents";

interface IconProps {
  variant: typeof svgIcons;
}
const Icon = ({ variant }: IconProps) => {
  return <div></div>;
};

export default Icon;
